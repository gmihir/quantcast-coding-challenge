# Quantcast Take-Home Submission

The test cases (in JUnit) can be executed with the following command:

```./cookie_test```

The program itself can be executed with the following command:
```./most_active_cookie [filename] [-d] [date]```